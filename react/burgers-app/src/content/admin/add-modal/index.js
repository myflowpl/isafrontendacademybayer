import Fab from '@mui/material/Fab';
import Icon from '@mui/material/Icon';

export const AddModalButton = () => {
    return <Fab variant="contained" color="primary">
        <Icon>add</Icon>
    </Fab>
}